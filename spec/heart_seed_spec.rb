require 'spec_helper'

describe HeartSeed do
  it 'has a version number' do
    expect(HeartSeed::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
